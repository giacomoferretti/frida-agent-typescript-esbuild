{
  "name": "com.example.app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --platform=node --outfile=./dist/agent.js --minify",
    "watch": "esbuild ./src/index.ts --bundle --platform=node --outfile=./dist/agent.js --minify --watch"
  },
  "dependencies": {
    "frida-hooks": "workspace:*"
  }
}
